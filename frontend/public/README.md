# Public Assets Folder

Static assets placed in this directory are served directly from `/` (e.g. `/favicon.ico`, `/logo.png`).
They are not processed by Next.js compiler, allowing direct URL reference.
